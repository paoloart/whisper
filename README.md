# whisper
Run GUI.py and register directly from your PC. The code run over the GPU, so computation during transcription could be long. Edit transcribe.py to change the model. By default you will find small. But with a good gaming GPU you can use also the "large" model.
